<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-addrbook">

	<!-- 댓글 쓰기 -->
	<insert id="createComment" parameterType="lx.team6.vo.PostVO">
		insert into comment(content)
		values(#{content})
	</insert>
	
	
	<!--댓글 가져오기 -->
	<select id="getCommentList" resultType="lx.team6.vo.PostVO">
			select content content
			from post
	</select>
	
	
	<!-- 삭제 -->
	<delete id="deleteComment" parameterType="int">
		delete from comment where comment_no = #{commentNo}
	</delete>
	
	
</mapper>