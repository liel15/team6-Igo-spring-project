<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-test">
	
	<select id="getListTest" resultType="lx.team6.vo.TestVO">
			select employee_id employeeId, first_name firstName, salary
			from employees
	</select>
	
	<select id="getTestById"  parameterType="int" resultType="lx.team6.vo.TestVO">
			select employee_id employeeId, first_name firstName, salary
			from employees
			where employeeId = #{employeeId}
	</select>
	
	<delete id="deleteTest" parameterType="int">
		delete from addrbook where ab_id = #{abId}
	</delete>
	
</mapper>